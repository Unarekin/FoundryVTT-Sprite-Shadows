export declare class TintFilter extends PIXI.Filter {
    #private;
    get color(): PIXI.ColorSource;
    set color(val: PIXI.ColorSource);
    get alpha(): number;
    set alpha(val: number);
    constructor(color?: PIXI.ColorSource);
}
